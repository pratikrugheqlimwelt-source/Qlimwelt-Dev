/**
 * Phase 3a stub — real export mapper in Phase 3c.
 */

export function toProductFootprint(_input: {
  companyId: string;
  calculationId: string;
}): never {
  throw new Error("toProductFootprint not implemented (Phase 3c)");
}
