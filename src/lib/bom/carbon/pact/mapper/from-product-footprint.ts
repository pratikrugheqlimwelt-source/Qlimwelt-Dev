/**
 * Phase 3a stub — real import mapper in Phase 4.
 */

import type { SupplierPcfRecord } from "../types";

export function fromProductFootprint(_input: {
  companyId: string;
  payload: unknown;
}): SupplierPcfRecord {
  throw new Error("fromProductFootprint not implemented (Phase 4)");
}
