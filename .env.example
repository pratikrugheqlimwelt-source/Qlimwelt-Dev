# Supabase (required for authentication)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NEVER add the Supabase service role key to frontend code or this file.

# Demo request form (Resend) — https://resend.com/api-keys
RESEND_API_KEY=
DEMO_REQUEST_EMAIL=pratikrughe.qlimwelt@gmail.com
RESEND_FROM=Qlimwelt <onboarding@resend.dev>

# --- Qlim AI (100% free open models) ---
# Local — Ollama is free forever on your PC:
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434/v1
# After: ollama create qlimwelt-carbon -f ollama/Modelfile
LLM_MODEL=qlimwelt-carbon

# Vercel / production — Groq FREE tier (no credit card, no paid plan):
# 1. Sign up: https://console.groq.com  (email only)
# 2. Create API key → paste into Vercel env vars
# 3. Redeploy
# Same carbon consultant prompt as Ollama. Rate-limited free, not billed.
# LLM_PROVIDER=groq
# GROQ_API_KEY=
# LLM_MODEL=llama-3.1-8b-instant
